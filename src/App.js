import React, { Component } from 'react';
import './App.scss';

import *component* from '*component path*';

class App extends Component {
  render() {
    return (
      <*component* />
    );
  }
}

export default App;
